import art

def add(n1, n2):
    return n1 + n2
def subtract(n1, n2):
    return n1 - n2
def multiply(n1, n2):
    return n1 * n2
def divide(n1, n2):
    return n1 / n2

staes = "a", "b", "c", "d"
operators = {'+' :add,
             '-' :subtract,
             '*' :multiply,
             '/' :divide
             }
cacs_ongoing = True
keep_going = 'y'

while cacs_ongoing == True:
    print(art.logo)
    num1 = float(input('what is your first number?\n'))
    for symbols in operators:
        print(symbols)
    op = input('what operator do you want to use?\n')
    num2 = float(input('what is your second number?\n'))
    final = operators[op](num1, num2)
    print(f'{num1} {op} {num2} = {final}')
    keep_going = input(f'Would you like to continue operations with {final}? "y" or "n"\n') .lower()
    while keep_going == 'y':
        for symbols in operators:
            print(symbols)
        op = input('what operator do you want to use?\n')
        num2 = float(input('what is your second number?\n'))
        final_continue = operators[op](final, num2)
        print(f'{final} {op} {num2} = {final_continue}')
        keep_going = input(f'Would you like to continue operations with {final_continue}? "y" or "n"\n')


